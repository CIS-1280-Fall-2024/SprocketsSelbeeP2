<?xml version="1.0" encoding="UTF-8" ?>
<Shell
    x:Class="SprocketsSelbeeP2.AppShell"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:local="clr-namespace:SprocketsSelbeeP2"
    Shell.FlyoutBehavior="Locked"
    Title="SprocketsSelbeeP2">

    <ShellContent
        Title="Sprockets Home"
        ContentTemplate="{DataTemplate local:MainPage}"
        Route="MainPage" />
    <ShellContent
        Title="Sprocket Order Form"
        ContentTemplate="{DataTemplate local:SprocketOrderForm}"
        Route="Sprocket Order Form" />

</Shell>
